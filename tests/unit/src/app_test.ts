/**
 * @file this file contains unit test for the app module.
 *  You must use Proxyquire together with sinon in order to unit test modules in isolation
 */

describe('app module', (): void => {
  it('should unit test the app module', async () => {
    return
  })
})
